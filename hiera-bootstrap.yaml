---

# This is a temporary hiera.yaml used only when executing the
# ./bootstrap.sh script

version: 5

defaults:
  data_hash: "yaml_data"
  datadir: "data"

hierarchy:
  - name: "Bootstrap hierarchy"
    paths:
      - "common.yaml"
